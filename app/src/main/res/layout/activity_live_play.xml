<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.c9mj.platform.live.ui.LivePlayActivity"
    android:orientation="vertical">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/live_play_top_layout"
        android:background="#000000"
        android:layout_weight="4">

        <SurfaceView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/surfaceview" />

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/live_play_progressbar"
            android:background="@color/divider"
            android:visibility="gone">

            <ProgressBar
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:padding="10dp"
                android:layout_gravity="center"
                style="@style/Widget.AppCompat.ProgressBar"
                android:theme="@style/CircularProgress"/>

        </FrameLayout>

        <master.flame.danmaku.ui.widget.DanmakuView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/danmuview"/>

        <include layout="@layout/layout_controller_landscape"
            android:visibility="gone"/>

        <include layout="@layout/layout_controller_portrait" />


    </FrameLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/live_play_bottom_layout"
        android:layout_weight="2"
        android:visibility="visible">

    </FrameLayout>
</LinearLayout>
